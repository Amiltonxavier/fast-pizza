export type geoResponseType = {
  continent: string;
  countryName: string;
  principalSubdivision: string;
  locality: string;
};

export type GeoTypes = {
  latitude: number;
  longitude: number;
};
